import { ReactNode } from "react";
import DashboardIcon from "@mui/icons-material/Dashboard";

export const breadcrumbIconMap: Record<string, ReactNode> = {
  dashboard: <DashboardIcon fontSize="small" />,
};
